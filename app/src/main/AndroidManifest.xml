<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="desenv.danilo.simpleinsta">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="desenv.danilo.simpleinsta.presentation.application.ApplicationApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name="desenv.danilo.simpleinsta.ui.ProfileActivity">
        </activity>

        <activity android:name="desenv.danilo.simpleinsta.ui.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="desenv.danilo.simpleinsta.ui.AuthenticationActivity">
        </activity>
    </application>

</manifest>